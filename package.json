{
  "name": "crime-reporting-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace scripts for running backend and frontend together",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:all": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
