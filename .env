# Root environment placeholders (optional aggregator)
# Keep real secrets in backend/.env and frontend/.env for local dev.

NODE_ENV=development
CLIENT_URL=http://localhost:5175
API_URL=http://localhost:5182
